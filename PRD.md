# 实验数据分析工作台 — 产品需求文档（PRD）

> 版本: v1.0 | 日期: 2026-08-06 | 状态: 已确认

---

## 1. 项目概述

### 1.1 项目定位

一个纯浏览器端运行的实验数据分析工作台，面向科研/实验人员，提供类似 SPSS + Origin 的数据分析、统计检验、图表生成能力。替代命令行 Python 脚本，提供图形化交互体验。

### 1.2 核心约束

- **仅本地运行**：浏览器打开，无后端服务器
- **数据持久化**：刷新、重启、关闭后数据不丢失，存储在本地浏览器
- **零网络依赖**：不需要联网、登录、云数据库、部署、手机端
- **单用户**：当前电脑使用者

### 1.3 数据规模

- 目标：中等数据量（几千到几万行，几 MB 到几十 MB）
- 格式：CSV、TSV、Excel (.xlsx/.xls)、JSON、TXT

---

## 2. 技术栈

| 层 | 选型 | 说明 |
|----|------|------|
| 框架 | React 18 + TypeScript | 复杂状态管理，长期可维护 |
| 构建 | Vite | 快速开发构建 |
| UI 组件库 | Ant Design 5 | Table、Form、Layout 对数据密集型应用友好 |
| 图表库 | ECharts 5（echarts-for-react） | 原生支持箱线图、热力图、等高线、3D 曲面等科研图表 |
| 统计计算 | jstat + ml.js + 自实现 | 覆盖描述统计、假设检验、回归分析 |
| 数据存储 | IndexedDB（Dexie.js） | 大容量本地存储，持久化数据集、图表、历史 |
| 路由 | React Router 6 | SPA 多页面路由 |
| 状态管理 | Zustand | 轻量、模块化，适合分析流水线的多步骤状态 |

---

## 3. 整体布局

```
┌──────────────────────────────────────────────────┐
│  TOP BAR: App Logo + 名称    │  当前数据状态指示  │
├────────┬─────────────────────────────────────────┤
│  📊 总览│                                          │
│  📥 导入│         主内容区                           │
│  🧹 清洗│      （每个模块在此渲染）                   │
│  📈 分析│                                          │
│  📉 图表│                                          │
│  📋 历史│                                          │
│  ⚙  设置│                                          │
├────────┴─────────────────────────────────────────┤
│  FOOTER: 版本号           │  已加载: xxx.csv (150行) │
└──────────────────────────────────────────────────┘
```

- **左侧边栏**：7 个模块入口，图标+文字，始终可见，当前页高亮
- **顶部栏**：左侧 App 名，右侧当前数据集名
- **底部栏**：版本号 + 当前数据集摘要信息
- **主内容区**：宽度自适应

---

## 4. 模块详细设计

### 4.1 首页总览

**定位**：打开即知"数据在哪、上次做到哪"的数据仪表盘。

**三区域布局**：

| 区域 | 内容 |
|------|------|
| 顶部：数据概览 + 快速操作 | 3 张状态卡片（样本量/变量数/导入时间）；4 个快速操作按钮（导入数据/描述统计/新建图表/最近分析） |
| 中部：数据预览 | 已加载数据的前 10 行表格，列头右键可设分组/忽略列 |
| 底部：最近分析 + 最近图表 | 两张卡片显示最近一次分析和图表摘要，可点击跳转历史详情 |

**空状态**：未加载数据时，状态卡片显示"--"，中部显示引导插图 + "请先导入实验数据" + [前往导入 →] 按钮。快速操作中仅"导入数据"可点击。

---

### 4.2 数据导入

**三步向导**，步骤条显示进度：

#### 步骤①：选择文件

- 拖拽/点击上传区，支持 CSV、TSV、Excel (.xlsx/.xls)、JSON、TXT
- 自动编码检测（UTF-8 / UTF-8-BOM / GB18030）
- 文件选中后自动进入步骤②

#### 步骤②：预览与清洗

- **清洗选项**（切换时预览即时刷新）：
  - [✓] 第一行作为列名
  - 跳过前 N 行
  - 分隔符选择（CSV: 逗号/制表符/分号/自动检测）
- **预览表格**：前 20 行，列头显示自动检测的类型（🔢数值/🔤分类），可点击切换；缺失值浅红色标记；表头冻结滚动
- 底部显示：列数、行数统计

#### 步骤③：确认导入

- 摘要确认面板：数据名、列数（数值N/分类M）、行数、缺失值统计
- 点击[确认导入] → 写入 IndexedDB → 跳转首页

---

### 4.3 数据清洗

**三标签页**结构：

#### 缺失值标签页

- 适用列选择（全部数值列/单列）
- 处理方式：删除含缺失行 / 填充（均值/中位数/指定值）/ 仅标记
- 显示受影响行数 + 实时预览（变更处高亮）
- [重置] / [应用更改]

#### 异常值标签页

- 检测方法：IQR（Q1 - k×IQR, Q3 + k×IQR），k 默认 1.5，可调
- 异常值在表格中橙色标记
- 处理方式：剔除 / 替换为边界值 / 保留

#### 列操作标签页

- 重命名列（双击编辑）
- 删除列（勾选确认）
- 类型转换（数值 ↔ 分类）
- 数据筛选：列 + 条件（大于/小于/等于/包含）+ 阈值

**通用规则**：所有更改先预览，[应用更改] 后写入存储，[重置] 放弃所有未保存修改。

---

### 4.4 实验数据分析（核心模块）

**三栏布局**：

```
┌──────────┬──────────────────┬──────────┐
│ 分析目录  │    变量配置区      │ 分析动作  │
│          │  (拖拽槽位)       │          │
│ ▼ 描述统计│                  │ 🔍 运行   │
│ ▼ 假设检验│                  │          │
│ ▼ 建模   │                  │  α: 0.05 │
│          │                  │          │
├──────────┴──────────────────┴──────────┤
│  可用列栏: [浓度🔢] [温度🔢] ...        │
└────────────────────────────────────────┘
```

#### 分析功能清单

**描述统计组**：
- 描述统计（均值/SD/中位数/极值/四分位数/峰度/偏度）
- 频数统计（计数+占比）
- 正态性检验（Shapiro-Wilk / K-S，含 Q-Q 图）
- 分组统计（按分组变量计算描述统计量）

**假设检验组**：
- 独立样本 t 检验（Welch，含 Cohen's d）
- 配对 t 检验（含差值置信区间）
- 单因素 ANOVA（含 Tukey HSD 事后比较）

**建模与高级分析组**：
- 相关矩阵（Pearson/Spearman/Kendall，热力图+显著性星号）
- 线性回归 OLS（R²、调整 R²、残差诊断图）
- 非线性拟合（线性/指数/幂函数/Gaussian，参数表+拟合曲线+残差图）
- 响应面分析 RSM（二次模型+等高线+3D曲面，可旋转）
- 主成分分析 PCA（碎石图+载荷矩阵+得分散点图）

#### 交互流程

1. **选分析类型**：左侧折叠目录点击，右侧变量槽位随之变化
2. **配变量**：从底部"可用列"栏拖拽/双击列名到上方槽位；无效拖放红色闪烁拒绝
3. **运行看结果**：运行后在配置区下方展开结果区（结果表格 + 结论文字）
4. **结果操作**：保存图表到模块 4；自动保存到历史（模块 5）

#### 变量槽位映射

| 分析类型 | 所需槽位 |
|---------|---------|
| 描述统计 | 变量（多选） |
| 频数统计 | 分类列（单选） |
| 正态检验 | 数值列（多选） |
| 分组统计 | 数值列（多选）+ 分组列（单选） |
| t检验 | 数值列（单选）+ 分组列（单选） |
| ANOVA | 数值列（单选）+ 分组列（单选） |
| 相关矩阵 | 数值列（多选） |
| 线性回归 | 因变量（单选）+ 自变量（多选） |
| 非线性拟合 | 自变量（单选）+ 因变量（单选）+ 模型选择 |
| 响应面分析 | 因素列（2-3个）+ 响应列（1个） |
| PCA | 数值列（多选） |

---

### 4.5 实验图表（独立图表工作台）

**两层结构**：画廊（列表态） → 编辑器（详情态），同页面内切换。

#### A. 图表画廊

- **布局**：网格卡片，每张显示缩略图+标题+来源+时间
- **筛选**：按图表类型（柱状图/箱线图/散点图/热力图/...）
- **排序**：最新/按名称
- **操作**：新建图表 / 批量管理（删除）
- **空状态**：无图表时引导"从分析结果保存图表"或"新建图表"

#### B. 图表编辑器

- **图表渲染区**：ECharts 实时渲染，全尺寸
- **编辑面板**（右侧）：
  - 标题编辑
  - 坐标轴：X/Y 轴标签、Y 轴范围（自动/手动）
  - 配色切换（学术灰度 / 彩色方案）
  - 图例位置（上/下/左/右上/右下）
  - 字体大小
- **导出**：PNG（汇报）/ SVG（论文矢量）/ 源数据 CSV
- **删除**：删除此图表
- **自动保存**：离开编辑器时保存配置到 IndexedDB

#### 支持的图表类型

| 类别 | 图表 |
|------|------|
| 基础 | 柱状图、折线图、散点图、面积图 |
| 统计 | 箱线图（含散点叠加）、小提琴图、误差棒图（均值±SD/±SEM）、Q-Q 图 |
| 科学/高级 | 热力图（相关矩阵）、等高线图、3D 曲面图（响应面）、直方图（含 KDE） |

---

### 4.6 历史记录

**双栏布局**：左侧时间线 + 右侧详情面板。

#### 左侧时间线

- 按日期分组折叠（今天/昨天/更早日期）
- 每条目显示：时间、分析类型图标、分析的变量概要、关键结果摘要
- 当前选中条目高亮
- 搜索：按分析类型名/列名/备注模糊搜索
- 筛选：分析类型多选 + 日期范围选择器

#### 右侧详情面板

- 完整分析参数（数据源/变量/分组/模型类型）
- 结果表格 + 结论文字（与分析模块一致）
- **备注框**：可编辑文字，失焦自动保存
- **查看关联图表**：跳转图表模块并定位
- **删除**：确认弹窗，可选"同时删除关联图表"

#### 自动记录规则

- 每次在分析模块点击"运行"后自动记录完整快照
- 记录内容：时间戳、分析类型、参数配置、结果数据、关联图表 ID

---

### 4.7 设置

**卡片分组布局**：

| 分组 | 设置项 | 默认值 |
|------|--------|--------|
| 分析默认值 | 显著性水平 α | 0.05 |
| | 有效数字位数 | 3 |
| 图表默认值 | 默认配色 | 学术灰度 |
| | 导出格式 | SVG |
| 历史记录 | 自动清理开关 | 开启 |
| | 保留天数 | 90 |
| 数据管理 | 存储统计（数据集数/图表数/记录数/占用估算）| — |
| | 导出全部数据（JSON 备份）| — |
| | 清空全部数据（三重点击确认）| — |
| 关于 | 版本号、使用说明 | v1.0 |

- 所有设置项即时生效，debounce 500ms 写入 localStorage
- "清空全部数据"：需输入"确认清空"方可执行

---

## 5. 跨模块流程

### 5.1 数据从导入到出图完整链路

```
导入(4.2) → 清洗(4.3) → 分析(4.4) → 图表(4.5)
  │            │            │            │
  └────────────┴────────────┴────────────┘
                     ↓
                历史记录(4.6)
              （自动记录每一步）
```

### 5.2 模块间跳转

| 从 → 到 | 触发方式 | 携带上下文 |
|---------|---------|-----------|
| 首页 → 导入 | 按钮/引导 | 无 |
| 首页 → 分析 | 快速操作 | 无 |
| 首页 → 图表 | 快速操作 | 无 |
| 首页 → 历史 | 最近卡片点击 | 定位到对应条目 |
| 分析 → 图表 | 保存图表按钮 | 图表配置+数据 |
| 分析 → 历史 | 自动记录 | 完整分析快照 |
| 历史 → 图表 | 查看关联图表 | 定位到对应图表 |
| 历史 → 分析 | 重新分析 | 预填变量配置 |

---

## 6. 数据存储方案

### 6.1 存储层级

| 存储目标 | 技术 | 内容 |
|---------|------|------|
| 数据集 | IndexedDB (Dexie.js) | 原始数据 + 列元信息（类型、标签） |
| 图表 | IndexedDB (Dexie.js) | ECharts 配置对象 + 缩略图 base64 |
| 历史记录 | IndexedDB (Dexie.js) | 分析快照（参数+结果+关联图表 ID） |
| 设置 | localStorage | 用户偏好（α值、配色、导出格式等） |

### 6.2 数据流

```
用户操作 → Zustand Store → Dexie.js → IndexedDB
                ↓
         React 组件重新渲染
```

---

## 7. V1 范围边界

### 7.1 V1 完成 ✅

- 全部 7 个模块（总览、导入、清洗、分析、图表、历史、设置）
- 分析功能：描述统计、频数统计、正态性检验（含 Q-Q 图）、分组统计、独立/配对 t 检验、单因素 ANOVA（含 Tukey HSD）、相关矩阵、线性回归、非线性拟合（4 种模型）、响应面分析、PCA
- 图表功能：全部 10 种图表类型，编辑器微调，导出 PNG/SVG/CSV
- 历史记录：自动记录、时间线、搜索筛选、备注、回溯跳转
- 设置：全部列出的设置项

### 7.2 暂不纳入 V1 ❌

| 内容 | 规划 |
|------|------|
| 多因素 ANOVA | V2 |
| 非参数检验（Mann-Whitney/Kruskal-Wallis/Wilcoxon） | V2 |
| 卡方检验 | V2 |
| 图表动画/过渡效果 | V2 |
| 数据导出为 Excel 格式 | V2（V1 支持 CSV） |
| 键盘快捷键 | V2 |

---

## 8. 参考

- 现有 Python 代码：`C:\Users\Administrator\Desktop\AItool\SPSS_Origin_Analysis.py`
  - 分析逻辑参考，JS 重写
  - 编码检测逻辑继承（UTF-8/GB18030 兼容）
  - 响应面分析算法继承（二次多项式 + contour + 3D surface）

---

> 本文档为 V1 开发唯一依据。所有页面结构、交互行为、数据流均以本文档为准。
